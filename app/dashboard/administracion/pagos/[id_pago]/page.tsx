"use client";
import {use,useEffect,useState} from "react";import MiddleNav from "@/app/general_components/componentes_recurrentes/MiddleNav";
export default function PagoPage({params}:{params:Promise<{id_pago:string}>}){const {id_pago}=use(params),[row,setRow]=useState<any>(null);useEffect(()=>{fetch(`/api/v1/admin/pagos-proveedores/${encodeURIComponent(id_pago)}`).then(r=>r.json()).then(setRow)},[id_pago]);return <div className="min-h-screen bg-gray-100"><MiddleNav tituloprincipal={`Pago ${id_pago}`}/><main className="p-6 lg:p-12"><section className="mx-auto max-w-3xl rounded-xl bg-white p-6 shadow">{!row?<p>Cargando...</p>:<dl className="grid gap-4 sm:grid-cols-2">{Object.entries(row).map(([k,v])=><div key={k}><dt className="text-xs font-semibold uppercase text-gray-400">{k}</dt><dd className="break-words">{String(v??"—")}</dd></div>)}</dl>}</section></main></div>}
